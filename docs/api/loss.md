# 損失関数

損失関数を提供します。

`ml_networks.torch.loss`（PyTorch）と`ml_networks.jax.loss`（JAX）の両方で提供されています。

## focal_loss

::: ml_networks.torch.loss.focal_loss

## binary_focal_loss

::: ml_networks.torch.loss.binary_focal_loss

## charbonnier

::: ml_networks.torch.loss.charbonnier

## FocalFrequencyLoss

::: ml_networks.torch.loss.FocalFrequencyLoss

## kl_divergence

::: ml_networks.torch.loss.kl_divergence

## kl_balancing

::: ml_networks.torch.loss.kl_balancing
